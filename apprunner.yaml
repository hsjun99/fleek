version: 1.0
runtime: nodejs12
build:
  commands:    
    build:
      - npm install            
      - npm install -g pm2  
    post-build:
      - pm2 link bdiv1i5usl8fmlu 57apckoyu0h0c3n               
run:                              
  command: pm2 ./bin/www -i max
  network:
    port: 3000